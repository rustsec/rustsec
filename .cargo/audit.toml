[advisories]
ignore = [
    # gix-worktree-state nonexclusive checkout sets executable files world-writable
    # TODO(tarcieri): upgrade `gix` (#1322)
    "RUSTSEC-2025-0001",
]

show = ["unmaintained", "unsound"]
fatal = ["critical"]

[output]
quiet = false
deny = ["warnings"]
