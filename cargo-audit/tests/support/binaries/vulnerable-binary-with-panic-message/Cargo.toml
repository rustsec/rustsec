[package]
name = "vulnerable-binary-with-panic-message"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace]

[dependencies]
chrono = { version = ">= 0.4, <= 0.4.19" }

[profile.release]
strip = true